/**
 * Google AdSense config. Replace with your AdSense client ID and ad unit slot IDs.
 * Get these from https://www.google.com/adsense/ → Account → Ads → By ad unit.
 */
export const ADSENSE_CLIENT_ID = 'ca-pub-9094458406112621'

/** Set false to hide all ad slots (no empty space). Set true when you want to show ads. */
export const SHOW_ADS = false

/** Set true to use test ads (data-adtest="on"); test impressions don't count. Set false for production. */
export const AD_TEST_MODE = false

/** Slot for the banner below the subtitle (above the form). */
export const AD_SLOT_TOP = '2101617670'
/** Slot for the ad between totals and state/city when layout is stacked. */
export const AD_SLOT_MIDDLE = '6182566550'
/** Slot for the banner above the footer. */
export const AD_SLOT_FOOTER = '8475454333'
